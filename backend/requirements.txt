# cognee==0.1.17 manages: fastapi, uvicorn, openai, httpx, aiofiles, websockets, pydantic
# We only pin packages cognee does NOT touch.
cognee==0.1.17
python-dotenv==1.0.1
python-multipart==0.0.9
pydantic-settings>=2.0.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Rate limiting
slowapi==0.1.9

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
